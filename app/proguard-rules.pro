# Keep Room-generated code and kotlinx.serialization models — the backup file
# format depends on stable (de)serialization of these classes.
-keep class com.lexis.words.data.** { *; }
-keepattributes *Annotation*
